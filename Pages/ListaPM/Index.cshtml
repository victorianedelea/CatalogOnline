@page
@model CatalogOnline.Pages.ListaPM.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ListaProfesoriMaterie[0].Materie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ListaProfesoriMaterie[0].Profesor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ListaProfesoriMaterie) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Materie.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Profesor.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
