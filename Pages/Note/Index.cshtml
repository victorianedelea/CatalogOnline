@page
@model CatalogOnline.Pages.Note.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nota[0].Valoare)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nota[0].Materie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nota[0].Student)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Nota) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Valoare)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Materie.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
